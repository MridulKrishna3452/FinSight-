# Screenshots

Placeholder directory for portfolio screenshots. Suggested captures once the
app is running locally with demo data seeded (`make seed`):

- `dashboard.png` — `/dashboard` (summary cards, recent transactions, risk alerts, budget progress, insight cards)
- `transactions.png` — `/transactions` (filters, table, suspicious-row highlighting)
- `budgets.png` — `/budgets` (green/yellow/red progress cards)
- `insights.png` — `/insights` (category donut, income vs. expense, forecast)
- `alerts.png` — `/alerts` (suspicious-transaction and budget-threshold alerts)
- `import.png` — `/import` (CSV preview + async job status)

Reference them from the main [README](../../README.md) once added.
